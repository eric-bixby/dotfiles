# My dotfiles

I am using [GNU Stow](https://www.gnu.org/software/stow/) to symlink
my dotfiles to their correct locations in my `$HOME` directory.

## Install

1. Clone the home-scripts repo
2. Install the dependencies using the install scripts in the 'BASH' folder of the home-scripts repo
3. Run `install.sh` in this repo

